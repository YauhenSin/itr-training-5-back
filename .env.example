# Database connection URL
DATABASE_URL="postgresql://username:password@localhost:5432/dbname?schema=public"

# JWT secret for signing access tokens
JWT_SECRET="your-jwt-secret"

# Allowed frontend origin for CORS (include protocol)
FRONTEND_ORIGIN="http://localhost:3001"

# SMTP credentials (example: Ethereal test account)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=aa4cf87cc207a4
SMTP_PASSWORD=6217f1556c5d19
EMAIL_FROM="Your App <no-reply@ethereal.email>"

# Base URL for activation links (frontend route that handles activation)
ACTIVATION_LINK_BASE="http://localhost:3001/activate"
